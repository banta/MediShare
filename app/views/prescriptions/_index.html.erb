<table class="table table-hover">
  <tr>
    <th>Name</th>
    <th>Actions</th>
  </tr>

<% @prescriptions.each do |prescription| %>
  <tr>
    <td><%= prescription.name unless prescription.name.nil? %></td>
    <td>
          <%= link_to t('.show', :default => t("helpers.links.show")),
                      prescription, :class => 'btn btn-mini' %>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_prescription_path(prescription), :class => 'btn btn-mini' %>
          <%= link_to 'Destroy', prescription,
                      :method => :delete,
                      :class => "btn btn-mini btn-danger", 
                      :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) %>
        </td>
  </tr>
<% end %>
</table>

<br />

<%#= link_to 'Add Disease', new_prescription_path, :class => "btn btn-primary" %>
